# Makefile options for the INTEL compiler, optimized

# Fortran compiler
FC=ifort
# Options for Fortran compiler:
FFLAGS= -warn nousage -O -fp-model precise -save -assume byterecl -I../include 
# "ranlib" command: if it is not needed, use "RANLIB=touch"
RANLIB=ranlib
VPATH=../include

